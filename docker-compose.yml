version: "3.9"

services:
  jupyter:
    build: .
    container_name: jupyter-uv
    ports:
      - "8888:8888"
    environment:
      - JUPYTER_TOKEN=${JUPYTER_TOKEN:-mysecret}
    volumes:
      - ./notebooks:/workspace
      - ./uv-cache:/root/.uv
    mem_limit: 48g
